#include "main.h"

/**
  *_isdigit - function that checks for a digit (0 through 9)
  *@c: integer to check
  *
  *Return: 1 if c is a digit, 0 if not
  */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
